## Packages
framer-motion | Page transitions, scroll animations, and interactive hover effects
lucide-react | High quality vector icons for automotive services

## Notes
- 100% static site as requested. No API or backend calls are implemented.
- The contact form simulates submission natively on the frontend via a timeout and toast.
- All routing is handled purely client-side via Wouter.
- A dark automotive theme is applied natively via CSS variables.
